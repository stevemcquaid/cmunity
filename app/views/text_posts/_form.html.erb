<%= form_for(@text_post) do |f| %>
  <% if @text_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_post.errors.count, "error") %> prohibited this text_post from being saved:</h2>

      <ul>
      <% @text_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content_id %><br />
    <%= f.number_field :content_id %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
